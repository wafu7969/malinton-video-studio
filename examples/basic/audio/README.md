# 配音文件放这里

示例清单引用了 `audio/scene1.mp3` … `audio/scene5.mp3`。

这个目录里没有实际音频文件，所以示例启动后：

- 「当前分镜配音」会显示文件路径，但播放是静音的
- 时间轴改用 `requestAnimationFrame` 时钟驱动，进度条、分镜高亮和字幕高亮**依然正常工作**

想听声音，把任意 mp3 按 `scene1.mp3` … `scene5.mp3` 命名放进来即可，无需改清单。
